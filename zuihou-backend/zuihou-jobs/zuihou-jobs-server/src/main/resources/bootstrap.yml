spring:
  application:
    name: zuihou-jobs-server
  cache:
    type: GENERIC

logging:
  file:
    path: /Users/san/work/san/web/zuihou-admin-cloud/data/projects/logs
    name: ${logging.file.path}/${spring.application.name}/root.log

